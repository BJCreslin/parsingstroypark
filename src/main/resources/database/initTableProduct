CREATE TABLE IF NOT EXISTS products (
  id          SERIAL PRIMARY KEY,
  name VARCHAR(500)   NOT NULL,
  link VARCHAR (1000),
  cost  DECIMAL ,
  comment    VARCHAR(1000),
  code int,
  ourName VARCHAR (1000)
);